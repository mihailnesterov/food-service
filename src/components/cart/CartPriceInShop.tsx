import React, {FC} from 'react';

/**
 * Блок "Цена корзины в магазине" в корзине
 * @returns JSX
 */
const CartPriceInShop: FC = () => {

	return(
        <div className="cart-price-in-shop">
            <h4>Что здесь должно быть...?</h4>
        </div>
    );
};

export default CartPriceInShop;